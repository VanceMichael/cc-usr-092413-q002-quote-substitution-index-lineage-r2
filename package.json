{
  "name": "consumer-quote-ledger",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test",
    "build": "node --check src/server.js && node --check src/quote-event.js"
  },
  "engines": {
    "node": ">=22"
  }
}
